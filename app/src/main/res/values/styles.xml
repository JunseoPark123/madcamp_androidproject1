<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- BottomNavigationView style without ripple effect -->
    <style name="NoRippleBottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="android:textSize">8sp</item>
    </style>
    <style name="BottomNavigationViewTextStyle" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">8sp</item>
        <!-- 여기에 더 많은 스타일 속성을 추가할 수 있습니다 -->
    </style>

</resources>
